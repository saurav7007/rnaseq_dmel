params {
    // default parameters
    outdir = "./results"
}

profiles {
    docker {
        docker.enabled = true
        process.executor = "local"
        process.cpus = 8
        process.memory = "16 GB"
        //process.time = "30m"
    }
}
